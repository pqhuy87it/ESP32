//>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>> "Adafruit_I2CDevice.h: No such file or directory" problem.
// When using the code in this project, the error message "Adafruit_I2CDevice.h: No such file or directory" appears in the Arduino IDE, 
// the solution is to install the "Adafruit Bus IO Library".
//
// You can install it from the "Library Manager" in the Arduino IDE, with the search keyword "Adafruit BusIO library".
//
// You can also install it manually, please download the library file here: https://github.com/adafruit/Adafruit_BusIO
//<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<






//>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>> 01_Test_BME280_Serial
/***************************************************************************
  This is a library for the BME280 humidity, temperature & pressure sensor

  Designed specifically to work with the Adafruit BME280 Breakout
  ----> http://www.adafruit.com/products/2650

  These sensors use I2C or SPI to communicate, 2 or 4 pins are required
  to interface. The device's I2C address is either 0x76 or 0x77.

  Adafruit invests time and resources providing this open source code,
  please support Adafruit andopen-source hardware by purchasing products
  from Adafruit!

  Written by Limor Fried & Kevin Townsend for Adafruit Industries.
  BSD license, all text above must be included in any redistribution
  See the LICENSE file for details.
 ***************************************************************************/

// This code is written based on the file "bme280test.ino" in the Adafruit BME280 Library.

//----------------------------------------Including Libraries.
#include <Wire.h>
//#include <SPI.h>  //--> Enable or use this library if your BME280 sensor module uses SPI communication.
#include <Adafruit_Sensor.h>
#include <Adafruit_BME280.h>
//----------------------------------------

//----------------------------------------Defines PINs if the BME280 sensor module uses SPI communication.
// Activate the code below if your BME280 sensor module uses SPI communication.
//#define BME_SCK   18
//#define BME_MISO  19
//#define BME_MOSI  23
//#define BME_CS    5
//----------------------------------------

// Defines the sea level pressure value. By default this is "1013.25". Please use the sea level pressure value at your location to be more accurate.
//#define SEALEVELPRESSURE_HPA (1013.25)
#define SEALEVELPRESSURE_HPA (1020.00)

//----------------------------------------Initialize the BME280 sensor based on the communication used. Select one of the options below.
Adafruit_BME280 bme; //--> I2C
//Adafruit_BME280 bme(BME_CS); //--> hardware SPI
//Adafruit_BME280 bme(BME_CS, BME_MOSI, BME_MISO, BME_SCK); //--> software SPI
//----------------------------------------

unsigned long delayTime;

//________________________________________________________________________________VOID SETUP()
void setup() {
  // put your setup code here, to run once:
  
  Serial.begin(115200);
  Serial.println();
  delay(1000);

  Serial.println();
  Serial.println(F("BME280 test"));
  
  //----------------------------------------Start accessing the BME280 sensor module.
  unsigned status;
  
  // By default the settings to start accessing the BME280 sensor module are :
  // - status = bme.begin();
  //
  // However, if the settings above don't work or 
  // you get the error message "Could not find a valid BME280 sensor, check wiring, address, sensor ID!" in the serial monitor,
  // then use the settings below :
  // - status = bme.begin(0x76);
  //
  // If address "0x76" does not work, use address "0x77", because generally BME280 sensor modules use one of those addresses.
  //
  
  //status = bme.begin();
  status = bme.begin(0x76);  
  
  // You can also pass in a Wire library object like &Wire2
  // status = bme.begin(0x76, &Wire2)
  
  if (!status) {
    Serial.println();
    Serial.println("Could not find a valid BME280 sensor, check wiring, address, sensor ID!");
    Serial.print("SensorID was: 0x"); Serial.println(bme.sensorID(),16);
    Serial.print("        ID of 0xFF probably means a bad address, a BMP 180 or BMP 085\n");
    Serial.print("   ID of 0x56-0x58 represents a BMP 280,\n");
    Serial.print("        ID of 0x60 represents a BME 280.\n");
    Serial.print("        ID of 0x61 represents a BME 680.\n");
    while (1) delay(10);
  }

  // If you have tried all the settings above, 
  // but you still get the error message "Could not find a valid BME280 sensor, check wiring, address, sensor ID!" in serial monitor, 
  // then try visiting and reading this website: https://randomnerdtutorials.com/solved-could-not-find-a-valid-bme280-sensor/
  //----------------------------------------

  Serial.println();
  Serial.println("-- Default Test --");
  delayTime = 2000;

  Serial.println();
}
//________________________________________________________________________________

//________________________________________________________________________________VOID LOOP()
void loop() { 
  // put your main code here, to run repeatedly:
  
  printValues();
  delay(delayTime);
}
//________________________________________________________________________________

//________________________________________________________________________________printValues()
void printValues() {
  Serial.println();
  Serial.println("--------------");
  Serial.print("Temperature = ");
  Serial.print(bme.readTemperature());
  Serial.println(" °C");

  Serial.print("Humidity = ");
  Serial.print(bme.readHumidity());
  Serial.println(" %");

  Serial.print("Pressure = ");
  Serial.print(bme.readPressure() / 100.0F);
  Serial.println(" hPa");

  Serial.print("Approx. Altitude = ");
  Serial.print(bme.readAltitude(SEALEVELPRESSURE_HPA));
  Serial.println(" m");
  Serial.println("--------------");
}
//________________________________________________________________________________
//<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<






//>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>> 02_BME280_with_LCD_20x4_I2C
//----------------------------------------Including Libraries.
#include <Wire.h>
#include <Adafruit_Sensor.h>
#include <Adafruit_BME280.h>
#include <LiquidCrystal_I2C.h>
//----------------------------------------

//----------------------------------------Defines the location number for the custom character to be displayed on the LCD.
#define temp_CustomChar   0
#define humd_CustomChar   1
#define pres_CustomChar   2
#define altd_CustomChar   3
#define degree_CustomChar 4
//----------------------------------------

// Defines the sea level pressure value. By default this is "1013.25". Please use the sea level pressure value at your location to be more accurate.
//#define SEALEVELPRESSURE_HPA (1013.25)
#define SEALEVELPRESSURE_HPA (1020.00)

float temp_Val, humd_Val, pres_Val, altd_Val;

// Array variable for temperature icon characters that will be displayed on the LCD.
byte temp_CustomChar_Array[] = {
  B00100,
  B01010,
  B01010,
  B01110,
  B11111,
  B11111,
  B11111,
  B01110
};

// Array variable for humidity icon characters that will be displayed on the LCD.
byte humd_CustomChar_Array[] = {
  B00100,
  B00100,
  B01010,
  B01010,
  B10001,
  B10001,
  B10001,
  B01110
};

// Array variable for pressure icon characters that will be displayed on the LCD.
byte pres_CustomChar_Array[] = {
  B00100,
  B10001,
  B00100,
  B00100,
  B00100,
  B01110,
  B01110,
  B00100
};

// Array variable for altitude icon characters that will be displayed on the LCD.
byte altd_CustomChar_Array[] = {
  B00100,
  B01110,
  B10101,
  B00100,
  B00100,
  B10101,
  B01110,
  B00100
};

// Array variable for degree symbol characters to be displayed on the LCD.
byte degree_CustomChar_Array[] = {
  B00110,
  B01001,
  B01001,
  B00110,
  B00000,
  B00000,
  B00000,
  B00000
};

// Initialize the BME280 sensor.
Adafruit_BME280 bme;

// Initialize the LiquidCrystal_I2C lib as "lcd" and set the LCD I2C address to 0x27 and set the LCD configuration to 20 x 4.
// In general, the address of a 20x4 I2C LCD is "0x27".
// However, if the address "0x27" doesn't work, you can find out the address with "i2c_scanner". Look here : https://playground.arduino.cc/Main/I2cScanner/
LiquidCrystal_I2C lcd(0x27, 20, 4);

//________________________________________________________________________________VOID SETUP()
void setup() {
  // put your setup code here, to run once:
  
  // Initialize lcd.
  lcd.init();
  // Turn on the LED backlight on the LCD.
  lcd.backlight();
  
  lcd.createChar(temp_CustomChar, temp_CustomChar_Array);
  lcd.createChar(humd_CustomChar, humd_CustomChar_Array);
  lcd.createChar(pres_CustomChar, pres_CustomChar_Array);
  lcd.createChar(altd_CustomChar, altd_CustomChar_Array);
  lcd.createChar(degree_CustomChar, degree_CustomChar_Array);
  
  // Clean the LCD display.
  lcd.clear();

  delay(500);

  //----------------------------------------Start accessing the BME280 sensor module.
  unsigned status;

  //status = bme.begin();
  status = bme.begin(0x76);
  
  if (!status) {
    lcd.setCursor(0,0);
    lcd.print("Could not find");
    lcd.setCursor(0,1);
    lcd.print("a valid BME280");
    lcd.setCursor(0,2);
    lcd.print("sensor. Check wiring");
    lcd.setCursor(0,3);
    lcd.print("& sensor address.");
    
    while(1) delay(10);
  }
  //----------------------------------------
}
//________________________________________________________________________________

//________________________________________________________________________________VOID LOOP()
void loop() { 
  // put your main code here, to run repeatedly:

  // Get BME280 sensor data.
  temp_Val = bme.readTemperature();
  humd_Val = bme.readHumidity();
  pres_Val = bme.readPressure() / 100.0F;
  altd_Val = bme.readAltitude(SEALEVELPRESSURE_HPA);

  // Displays the temperature value on the LCD.
  lcd.setCursor(0,0);
  lcd.write(temp_CustomChar);
  lcd.setCursor(2,0);
  lcd.print("Temp: ");
  lcd.setCursor(8,0);
  lcd.print("          ");
  lcd.setCursor(8,0);
  lcd.print(temp_Val);
  lcd.setCursor(18,0);
  lcd.write(degree_CustomChar);
  lcd.setCursor(19,0);
  lcd.print("C");

  // Displays the humidity value on the LCD.
  lcd.setCursor(0,1);
  lcd.write(humd_CustomChar);
  lcd.setCursor(2,1);
  lcd.print("Humd: ");
  lcd.setCursor(8,1);
  lcd.print("           ");
  lcd.setCursor(8,1);
  lcd.print(humd_Val);
  lcd.setCursor(19,1);
  lcd.print("%");

  // Displays the pressure value on the LCD.
  lcd.setCursor(0,2);
  lcd.write(pres_CustomChar);
  lcd.setCursor(2,2);
  lcd.print("Pres: ");
  lcd.setCursor(8,2);
  lcd.print("         ");
  lcd.setCursor(8,2);
  lcd.print(pres_Val);
  lcd.setCursor(17,2);
  lcd.print("hPa");

  // Displays the altitude value on the LCD.
  lcd.setCursor(0,3);
  lcd.write(altd_CustomChar);
  lcd.setCursor(2,3);
  lcd.print("Altd: ");
  lcd.setCursor(8,3);
  lcd.print("           ");
  lcd.setCursor(8,3);
  lcd.print(altd_Val);
  lcd.setCursor(19,3);
  lcd.print("m");

  delay(2000);
}
//________________________________________________________________________________
//<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<






//>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>> 03_ESP32_Simple_Weather_Station_With_BME280_Web_Server_SM
//----------------------------------------Including Libraries.
#include <AsyncTCP.h>
#include <ESPAsyncWebServer.h>
#include <Arduino_JSON.h>
#include <Wire.h>
#include <Adafruit_Sensor.h>
#include <Adafruit_BME280.h>
#include <LiquidCrystal_I2C.h>
#include "PageIndex.h"
//----------------------------------------

//----------------------------------------Defines the location number for the custom character to be displayed on the LCD.
#define temp_CustomChar   0
#define humd_CustomChar   1
#define pres_CustomChar   2
#define altd_CustomChar   3
#define degree_CustomChar 4
//----------------------------------------

// Defines the sea level pressure value. By default this is "1013.25". Please use the sea level pressure value at your location to be more accurate.
//#define SEALEVELPRESSURE_HPA (1013.25)
#define SEALEVELPRESSURE_HPA (1020.00)

//----------------------------------------Variable declaration for your network credentials.
const char* ssid = "REPLACE_WITH_YOUR_SSID";  //--> Your wifi name
const char* password = "REPLACE_WITH_YOUR_PASSWORD"; //--> Your wifi password
//----------------------------------------

float temp_Val, humd_Val, pres_Val, altd_Val;

// Variables for Millis/Timer.
unsigned long previousMillis = 0;
const long interval = 2000;

// Array variable for temperature icon characters that will be displayed on the LCD.
byte temp_CustomChar_Array[] = {
  B00100,
  B01010,
  B01010,
  B01110,
  B11111,
  B11111,
  B11111,
  B01110
};

// Array variable for humidity icon characters that will be displayed on the LCD.
byte humd_CustomChar_Array[] = {
  B00100,
  B00100,
  B01010,
  B01010,
  B10001,
  B10001,
  B10001,
  B01110
};

// Array variable for pressure icon characters that will be displayed on the LCD.
byte pres_CustomChar_Array[] = {
  B00100,
  B10001,
  B00100,
  B00100,
  B00100,
  B01110,
  B01110,
  B00100
};

// Array variable for altitude icon characters that will be displayed on the LCD.
byte altd_CustomChar_Array[] = {
  B00100,
  B01110,
  B10101,
  B00100,
  B00100,
  B10101,
  B01110,
  B00100
};

// Array variable for degree symbol characters to be displayed on the LCD.
byte degree_CustomChar_Array[] = {
  B00110,
  B01001,
  B01001,
  B00110,
  B00000,
  B00000,
  B00000,
  B00000
};

// Initialize the BME280 sensor.
Adafruit_BME280 bme;

// Initialize the LiquidCrystal_I2C lib as "lcd" and set the LCD I2C address to 0x27 and set the LCD configuration to 20 x 4.
// In general, the address of a 20x4 I2C LCD is "0x27".
// However, if the address "0x27" doesn't work, you can find out the address with "i2c_scanner". Look here : https://playground.arduino.cc/Main/I2cScanner/
LiquidCrystal_I2C lcd(0x27, 20, 4);

// Initialize JSONVar.
JSONVar JSON_All_Data_Received;

// Create AsyncWebServer object on port 80.
AsyncWebServer server(80);

// Create an Event Source on /events.
AsyncEventSource events("/events");

//________________________________________________________________________________get_BME280_sensor_data()
// Subroutine for getting data from the BME280 sensor which is then displayed on the LCD and Serial Monitor.
void get_BME280_sensor_data() {
  // Get BME280 sensor data.
  temp_Val = bme.readTemperature();
  humd_Val = bme.readHumidity();
  pres_Val = bme.readPressure() / 100.0F;
  altd_Val = bme.readAltitude(SEALEVELPRESSURE_HPA);

  // Prints BME280 sensor data to Serial Monitor.
  Serial.println();
  Serial.println("--------------BME280 sensor data");
  Serial.print("Temperature = ");
  Serial.print(temp_Val);
  Serial.println(" °C");

  Serial.print("Humidity = ");
  Serial.print(humd_Val);
  Serial.println(" %");

  Serial.print("Pressure = ");
  Serial.print(pres_Val);
  Serial.println(" hPa");

  Serial.print("Approx. Altitude = ");
  Serial.print(altd_Val);
  Serial.println(" m");
  Serial.println("--------------");

  // Displays the temperature value on the LCD.
  lcd.setCursor(0,0);
  lcd.write(temp_CustomChar);
  lcd.setCursor(2,0);
  lcd.print("Temp: ");
  lcd.setCursor(8,0);
  lcd.print("          ");
  lcd.setCursor(8,0);
  lcd.print(temp_Val);
  lcd.setCursor(18,0);
  lcd.write(degree_CustomChar);
  lcd.setCursor(19,0);
  lcd.print("C");

  // Displays the humidity value on the LCD.
  lcd.setCursor(0,1);
  lcd.write(humd_CustomChar);
  lcd.setCursor(2,1);
  lcd.print("Humd: ");
  lcd.setCursor(8,1);
  lcd.print("           ");
  lcd.setCursor(8,1);
  lcd.print(humd_Val);
  lcd.setCursor(19,1);
  lcd.print("%");

  // Displays the pressure value on the LCD.
  lcd.setCursor(0,2);
  lcd.write(pres_CustomChar);
  lcd.setCursor(2,2);
  lcd.print("Pres: ");
  lcd.setCursor(8,2);
  lcd.print("         ");
  lcd.setCursor(8,2);
  lcd.print(pres_Val);
  lcd.setCursor(17,2);
  lcd.print("hPa");

  // Displays the altitude value on the LCD.
  lcd.setCursor(0,3);
  lcd.write(altd_CustomChar);
  lcd.setCursor(2,3);
  lcd.print("Altd: ");
  lcd.setCursor(8,3);
  lcd.print("           ");
  lcd.setCursor(8,3);
  lcd.print(altd_Val);
  lcd.setCursor(19,3);
  lcd.print("m");
}
//________________________________________________________________________________ 

//________________________________________________________________________________Send_Data_to_WS()
// Subroutine for sending data to the web server.
void Send_Data_to_WS() {
  // Calls the get_BME280_sensor_data() subroutine.
  get_BME280_sensor_data();

  Serial.println();
  Serial.println("Send data to Web Server");
  
  //:::::::::::::::::: Enter the data into JSONVar(JSON_All_Data_Received).
  // Look at the file "PageIndex.h". Then look at the section "Processes the data received from the ESP32."
  // "temp_Val" is for "obj.temp_Val".
  // "humd_Val" is for "obj.humd_Val".
  // and so on.
  JSON_All_Data_Received["temp_Val"] = temp_Val;
  JSON_All_Data_Received["humd_Val"] = humd_Val;
  JSON_All_Data_Received["pres_Val"] = pres_Val;
  JSON_All_Data_Received["altd_Val"] = altd_Val;
  //:::::::::::::::::: 
  
  //:::::::::::::::::: Create a JSON String to hold all data received from the sender.
  String jsonString_Send_All_Data_received = JSON.stringify(JSON_All_Data_Received);
  //:::::::::::::::::: 
  
  //:::::::::::::::::: Sends all data received from the sender to the browser as an event ('allDataJSON').
  events.send(jsonString_Send_All_Data_received.c_str(), "allDataJSON", millis());
  //::::::::::::::::::  
}
//________________________________________________________________________________

//________________________________________________________________________________VOID SETUP()
void setup() {
  // put your setup code here, to run once:
  
  Serial.begin(115200);
  Serial.println();
  delay(1000);

  // Initialize lcd.
  lcd.init();
  // Turn on the LED backlight on the LCD.
  lcd.backlight();
  
  lcd.createChar(temp_CustomChar, temp_CustomChar_Array);
  lcd.createChar(humd_CustomChar, humd_CustomChar_Array);
  lcd.createChar(pres_CustomChar, pres_CustomChar_Array);
  lcd.createChar(altd_CustomChar, altd_CustomChar_Array);
  lcd.createChar(degree_CustomChar, degree_CustomChar_Array);
  
  // Clean the LCD display.
  lcd.clear();

  //----------------------------------------Start accessing the BME280 sensor module.
  unsigned status;

  //status = bme.begin();
  status = bme.begin(0x76); 
  
  if (!status) {
    Serial.println("Could not find a valid BME280 sensor, check wiring, address, sensor ID!");
    Serial.print("SensorID was: 0x"); Serial.println(bme.sensorID(),16);
    Serial.print("        ID of 0xFF probably means a bad address, a BMP 180 or BMP 085\n");
    Serial.print("   ID of 0x56-0x58 represents a BMP 280,\n");
    Serial.print("        ID of 0x60 represents a BME 280.\n");
    Serial.print("        ID of 0x61 represents a BME 680.\n");

    lcd.setCursor(0,0);
    lcd.print("Could not find");
    lcd.setCursor(0,1);
    lcd.print("a valid BME280");
    lcd.setCursor(0,2);
    lcd.print("sensor. Check wiring");
    lcd.setCursor(0,3);
    lcd.print("& sensor address.");
    
    while (1) delay(10);
  }
  //----------------------------------------
  
  //----------------------------------------Set Wifi to STA mode
  Serial.println();
  Serial.println("-------------");
  Serial.println("WIFI mode : STA");
  WiFi.mode(WIFI_STA);
  Serial.println("-------------");
  //---------------------------------------- 

  delay(100);

  //----------------------------------------Connect to Wi-Fi (STA).
  lcd.clear();
  lcd.setCursor(1,0);
  lcd.print("Connecting to WiFi");
  delay(1000);

  lcd.clear();
  
  Serial.println("------------");
  Serial.println("WIFI STA");
  Serial.print("Connecting to ");
  Serial.println(ssid);
  WiFi.begin(ssid, password);
  
  //:::::::::::::::::: The process of connecting ESP32 with WiFi Hotspot / WiFi Router.
  // The process timeout of connecting ESP32 with WiFi Hotspot / WiFi Router is 20 seconds.
  // If within 20 seconds the ESP32 has not been successfully connected to WiFi, the ESP32 will restart.
  // I made this condition because on my ESP32, there are times when it seems like it can't connect to WiFi, so it needs to be restarted to be able to connect to WiFi.
  
  int connecting_process_timed_out = 20; //--> 20 = 20 seconds.
  connecting_process_timed_out = connecting_process_timed_out * 2;
  while (WiFi.status() != WL_CONNECTED) {
    Serial.print(".");
    delay(500);
    if(connecting_process_timed_out > 0) connecting_process_timed_out--;
    if(connecting_process_timed_out == 0) {
      lcd.clear();
      lcd.setCursor(0,0);
      lcd.print("Failed to connect");
      lcd.setCursor(0,1);
      lcd.print("to WiFi.");
      lcd.setCursor(0,2);
      lcd.print("ESP32 will restart.");
      delay(1000);
      ESP.restart();
    }
  }

  lcd.clear();
  lcd.setCursor(0,0);
  lcd.print("Connected to");
  lcd.setCursor(0,1);
  lcd.print("WiFi successfully.");
  
  Serial.println("");
  Serial.println("WiFi connected");
  Serial.println("------------");

  delay(2000);

  lcd.clear();
  //:::::::::::::::::: 
  //---------------------------------------- 

  delay(500);

  //----------------------------------------Handle Web Server
  Serial.println();
  Serial.println("Setting Up the Main Page on the Server.");
  server.on("/", HTTP_GET, [](AsyncWebServerRequest *request){
    request->send_P(200, "text/html", MAIN_page);
  });
  //---------------------------------------- 

  //----------------------------------------Handle Web Server Events
  Serial.println();
  Serial.println("Setting up event sources on the Server.");
  events.onConnect([](AsyncEventSourceClient *client){
    if(client->lastId()){
      Serial.printf("Client reconnected! Last message ID that it got is: %u\n", client->lastId());
    }
    // send event with message "hello!", id current millis
    // and set reconnect delay to 10 second
    client->send("hello!", NULL, millis(), 10000);
  });
  //---------------------------------------- 

  //----------------------------------------Adding event sources on the Server.
  Serial.println();
  Serial.println("Adding event sources on the Server.");
  server.addHandler(&events);
  //---------------------------------------- 

  //----------------------------------------Starting the Server.
  Serial.println();
  Serial.println("Starting the Server.");
  server.begin();
  //----------------------------------------

  Serial.println();
  Serial.println("------------");
  Serial.print("ESP32 IP address : ");
  Serial.println(WiFi.localIP());
  Serial.println();
  Serial.println("Visit the IP Address above in your browser to open the main page.");
  Serial.println("------------");
  Serial.println();

  lcd.clear();
  lcd.setCursor(0,0);
  lcd.print("IP address :");
  lcd.setCursor(0,1);
  lcd.print(WiFi.localIP());

  delay(3000);

  lcd.clear();
}
//________________________________________________________________________________

//________________________________________________________________________________VOID LOOP()
void loop() { 
  // put your main code here, to run repeatedly:

  //----------------------------------------Millis/Timer to send data to the web server.
  // Data sending interval every 2000ms (2 seconds). Look at the value of the variable "interval".
  unsigned long currentMillis = millis();
  if (currentMillis - previousMillis >= interval) {
    previousMillis = currentMillis;

    // Calls the Send_Data_to_WS() subroutine.
    Send_Data_to_WS();
  }
  //----------------------------------------
}
//________________________________________________________________________________
//<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<






//>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>> 04_ESP32_Simple_Weather_Station_With_BME280_Web_Server_APM
//---------------------------------------- Access Point Declaration and Configuration.
const char* ssid = "ESP32_WS";  //--> access point name
const char* password = "helloesp32WS"; //--> access point password

IPAddress local_ip(192,168,1,1);
IPAddress gateway(192,168,1,1);
IPAddress subnet(255,255,255,0);
//----------------------------------------

//----------------------------------------Set Wifi to AP mode
Serial.println();
Serial.println("-------------");
Serial.println("WIFI mode : AP");
WiFi.mode(WIFI_AP);
Serial.println("-------------");
//---------------------------------------- 

//----------------------------------------Setting up ESP32 to be an Access Point.
Serial.println();
Serial.println("-------------");
Serial.println("Setting up ESP32 to be an Access Point.");
WiFi.softAP(ssid, password); //--> Creating Access Points
delay(1000);
Serial.println("Setting up ESP32 softAPConfig.");
WiFi.softAPConfig(local_ip, gateway, subnet);
Serial.println("-------------");
//----------------------------------------

Serial.println();
Serial.println("------------");
Serial.print("SSID name : ");
Serial.println(ssid);
Serial.print("IP address : ");
Serial.println(WiFi.softAPIP());
Serial.println();
Serial.println("Connect your computer or mobile Wifi to the SSID above.");
Serial.println("Visit the IP Address above in your browser to open the main page.");
Serial.println("------------");
Serial.println();

lcd.clear();
lcd.setCursor(0,0);
lcd.print("IP address :");
lcd.setCursor(0,1);
lcd.print(WiFi.softAPIP());
//<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<